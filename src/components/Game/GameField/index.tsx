import React from "react";

type GameFieldProps = {
  n: number;
  m: number;
  isUserFirst: boolean;
};

function GameField({ n, m, isUserFirst }: GameFieldProps): JSX.Element {
  return <></>;
}

export default GameField;
